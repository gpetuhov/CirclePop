package com.gpetuhov.android.circlepop;

public class GameControl {
    DrawCircleView mDrawCircleView;

    public GameControl(DrawCircleView drawCircleView) {
        mDrawCircleView = drawCircleView;
        initCircle();
    }

    private void initCircle() {

    }
}
